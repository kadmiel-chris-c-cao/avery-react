import React from 'react';
import { Grid } from '@mui/material';

const PairDescription = () => {
    return (
        <Grid container spacing={2} sx={{ backgroundColor: '#000', height: '100vh' }}>
            {/* Content goes here */}
        </Grid>
    );
};

export default PairDescription;
